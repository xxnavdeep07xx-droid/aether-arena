# ============================================
# Aether Arena - Environment Configuration
# ============================================
# Copy this file to .env and fill in the values.

# ------------------------------------------
# Database (PostgreSQL via Supabase)
# ------------------------------------------
# Supabase pooler URL (transaction mode)
DATABASE_URL=postgresql://user:password@aws-0-region.pooler.supabase.com:6543/postgres
# Direct connection URL (required for Prisma migrations)
DIRECT_URL=postgresql://user:password@db.region.supabase.co:5432/postgres

# ------------------------------------------
# Discord OAuth (Optional — Social Login)
# ------------------------------------------
# Create an app at https://discord.com/developers/applications
DISCORD_CLIENT_ID=your_discord_client_id
DISCORD_CLIENT_SECRET=your_discord_client_secret
DISCORD_REDIRECT_URI=https://your-domain.com/api/auth/discord/callback
DISCORD_TOKEN=your_discord_bot_token

# ------------------------------------------
# Session & Setup
# ------------------------------------------
# Secret for protecting the /api/setup endpoint
SETUP_SECRET=your_setup_secret_here

# ------------------------------------------
# App URL (Required for referrals & links)
# ------------------------------------------
# Your deployed domain (no trailing slash)
NEXT_PUBLIC_BASE_URL=https://your-domain.com

# ------------------------------------------
# Razorpay (Coming Soon — not active yet)
# ------------------------------------------
# Payments currently use Google Pay (manual verification)
# Razorpay will be enabled in a future update
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# ------------------------------------------
# Google Pay (Active — configured in Admin Settings)
# ------------------------------------------
# The GPay number is stored in PlatformSetting (database)
# Configure it via Admin Panel > Settings > Payments
# Default fallback: 9158396121

# ------------------------------------------
# Discord Bot (Optional — for mini-services)
# ------------------------------------------
DISCORD_BOT_TOKEN=your_discord_bot_token

# ------------------------------------------
# Redis (Optional — for rate limiting)
# ------------------------------------------
# Option 1: Full Redis URL
REDIS_URL=redis://default:password@host:6379
# Option 2: Individual vars
REDIS_HOST=your-redis-host
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password
# Option 3: Vercel KV / Upstash REST API
KV_REST_API_URL=https://your-kv-url.upstash.io
KV_REST_API_TOKEN=your_kv_token

# ------------------------------------------
# Email (Gmail SMTP — for email verification)
# ------------------------------------------
# Uses your Gmail account to send emails (500 free/day)
#
# Setup steps:
# 1. Go to https://myaccount.google.com/security
# 2. Enable 2-Step Verification (required for App Password)
# 3. Go to https://myaccount.google.com/apppasswords
# 4. Create an App Password for "Mail" → use that as GMAIL_APP_PASSWORD
#
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your_16_char_app_password_here
# Optional: Override the "From" display name/address
EMAIL_FROM=Aether Arena <your-email@gmail.com>

# ------------------------------------------
# Cron Secret (Required for Vercel Cron Jobs)
# ------------------------------------------
# Generate a random string: openssl rand -hex 32
CRON_SECRET=your_cron_secret_here
